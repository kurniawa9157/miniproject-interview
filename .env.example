# Variabel ini dipakai oleh docker-compose.yml (service db).
# Salin ke .env di root, samakan nilainya dengan DB_* di backend/.env.
DB_USER=postgres
DB_PASSWORD=ganti_password_kuat
DB_NAME=miniproject
